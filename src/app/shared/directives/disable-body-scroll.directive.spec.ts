import { DisableBodyScrollDirective } from './disable-body-scroll.directive';

describe('DisableBodyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableBodyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
