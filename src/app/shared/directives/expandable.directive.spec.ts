import { ExpandableDirective } from './expandable.directive';

describe('ExpandableDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandableDirective();
    expect(directive).toBeTruthy();
  });
});
