import { NavbarHeadingContentDirective } from './navbar-heading-content.directive';

describe('NavbarHeadingContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarHeadingContentDirective();
    expect(directive).toBeTruthy();
  });
});
